overwrite: true
schema: 'http://localhost:4000/graphql'
generates:
  src/adapter/sdk.ts:
    documents: 'src/adapter/operation.graphql'
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
